# PlanForge environment
# direnv will load these automatically when you cd into this directory

# Python virtual environment (uv)
# Uncomment when backend venv exists:
# source .venv/bin/activate

# Remind which stack is active
echo "📐 PlanForge — FastAPI + Next.js + PostgreSQL"
